/**
 * CalVer + product name only — safe to import from any route without pulling release JSX / router.
 */
export const productName = "Xamsa";

export const current = { year: 2026, month: 4, patch: 11 } as const;
